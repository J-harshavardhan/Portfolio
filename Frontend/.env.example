# Local development
VITE_API_URL=http://localhost:8000

# Production uses the same-origin Vercel API route by default. Leave this unset.
# Set this only when deliberately using a separately deployed backend.
# VITE_API_URL=https://your-backend-url.com
